version: "3.9"

services:
  ferienplanner:
    build: .
    container_name: ferienplanner
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
    restart: unless-stopped